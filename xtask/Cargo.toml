[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "Development task runner for synapse-core"

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive", "env"] }
anyhow = "1"
